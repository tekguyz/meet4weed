import { LoadingScreen } from "@/components/fallback/fallback-screens";

/** Outside the Frame, and while the Frame's own layout reads the member. */
export default function Loading() {
  return <LoadingScreen />;
}
